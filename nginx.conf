server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback: rotas do React Router são resolvidas pelo index.html
  location / {
    try_files $uri $uri/ /index.html;
  }
}
